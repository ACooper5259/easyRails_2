<nav class="navbar navbar-expand-lg navbar-dark sticky-top  mb-5" style="background-color: #004578;">
  <div class="container">
    <%= link_to image_tag("logos/white_logo.png", height: '50'), root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if user_signed_in? %>
          <li class="nav-item">
          <%# <% if admin?%> 
            <%= link_to('All bookings', bookings_path, class: "nav-link" ) %>
            <%# <% end %>
          </li>  
          <li class="nav-item">
            <%= link_to('Book ride', new_booking_path, class: "nav-link") %>
          </li>
          <li class="nav-item">
            <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "nav-link") %>
          </li>
          <% else %>
            <%= link_to('Login', new_user_session_path, class: "nav-link") %>
          <% end %>
        </li>
        
            
        
         
        
      </ul>
    </div>
  </div>
</nav>